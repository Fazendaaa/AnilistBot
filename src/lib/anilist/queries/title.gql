query ($id: Int, $type: MediaType) {
    Media (id: $id, type: $type) {
        status,
        siteUrl,
        countryOfOrigin,
        title {
            romaji
            native
            english
        }
    }
}
